---
import { ChevronDown, ArrowUpRight, Sun, Moon, Menu, X, Globe } from 'lucide-astro';
import { type Lang, localePath, getAlternateLang, stripLangPrefix } from '../i18n/config';
import { t } from '../i18n/ui';

interface Props {
  currentPath?: string;
  lang?: Lang;
}

const { currentPath = '', lang = 'zh' } = Astro.props;
const altLang = getAlternateLang(lang);
const altPath = localePath(altLang, currentPath);

interface NavItem {
  title: string;
  href?: string;
  external?: boolean;
  children?: NavItem[];
  dividerBefore?: boolean;
  megaMenu?: boolean;
  group?: boolean;
}

const lp = (path: string) => localePath(lang, path);

const navigation: NavItem[] = [
  { title: t(lang, 'nav.home'), href: lp('/') },
  { title: t(lang, 'nav.concepts'), href: lp('/concepts/') },
  {
    title: t(lang, 'nav.clusterManual'),
    megaMenu: true,
    children: [
      { title: t(lang, 'nav.group.lighting'), group: true },
      { title: t(lang, 'nav.cluster.onOff'), href: lp('/clusters/on-off/') },
      { title: t(lang, 'nav.cluster.levelControl'), href: lp('/clusters/level-control/') },
      { title: t(lang, 'nav.cluster.colorControl'), href: lp('/clusters/color-control/') },
      { title: t(lang, 'nav.group.closure'), group: true },
      { title: t(lang, 'nav.cluster.doorLock'), href: lp('/clusters/door-lock/') },
      { title: t(lang, 'nav.cluster.windowCovering'), href: lp('/clusters/window-covering/') },
      { title: t(lang, 'nav.cluster.valveConfig'), href: lp('/clusters/valve-configuration-and-control/') },
      { title: t(lang, 'nav.group.hvac'), group: true },
      { title: t(lang, 'nav.cluster.thermostat'), href: lp('/clusters/thermostat/') },
      { title: t(lang, 'nav.cluster.fanControl'), href: lp('/clusters/fan-control/') },
      { title: t(lang, 'nav.group.sensing'), group: true },
      { title: t(lang, 'nav.cluster.temperature'), href: lp('/clusters/temperature-measurement/') },
      { title: t(lang, 'nav.cluster.humidity'), href: lp('/clusters/relative-humidity-measurement/') },
      { title: t(lang, 'nav.cluster.occupancy'), href: lp('/clusters/occupancy-sensing/') },
      { title: t(lang, 'nav.cluster.airQuality'), href: lp('/clusters/air-quality/') },
      { title: t(lang, 'nav.group.safety'), group: true },
      { title: t(lang, 'nav.cluster.smokeCOAlarm'), href: lp('/clusters/smoke-co-alarm/') },
      { title: t(lang, 'nav.cluster.booleanState'), href: lp('/clusters/boolean-state/') },
      { title: t(lang, 'nav.group.appliance'), group: true },
      { title: t(lang, 'nav.cluster.operationalState'), href: lp('/clusters/operational-state/') },
      { title: t(lang, 'nav.cluster.laundryWasher'), href: lp('/clusters/laundry-washer-mode/') },
      { title: t(lang, 'nav.cluster.rvcRunMode'), href: lp('/clusters/rvc-run-mode/') },
      { title: t(lang, 'nav.group.media'), group: true },
      { title: t(lang, 'nav.cluster.mediaPlayback'), href: lp('/clusters/media-playback/') },
      { title: t(lang, 'nav.cluster.channel'), href: lp('/clusters/channel/') },
      { title: t(lang, 'nav.group.general'), group: true },
      { title: t(lang, 'nav.cluster.identify'), href: lp('/clusters/identify/') },
      { title: t(lang, 'nav.cluster.descriptor'), href: lp('/clusters/descriptor/') },
      { title: t(lang, 'nav.cluster.accessControl'), href: lp('/clusters/access-control/') },
      { title: t(lang, 'nav.group.node'), group: true },
      { title: t(lang, 'nav.cluster.basicInfo'), href: lp('/clusters/basic-information/') },
      { title: t(lang, 'nav.cluster.powerSource'), href: lp('/clusters/power-source/') },
      { title: t(lang, 'nav.cluster.timeSync'), href: lp('/clusters/time-synchronization/') },
      { title: t(lang, 'nav.group.commissioning'), group: true },
      { title: t(lang, 'nav.cluster.networkComm'), href: lp('/clusters/network-commissioning/') },
      { title: t(lang, 'nav.cluster.opCred'), href: lp('/clusters/operational-credentials/') },
      { title: t(lang, 'nav.group.diagnostics'), group: true },
      { title: t(lang, 'nav.cluster.otaProvider'), href: lp('/clusters/ota-software-update-provider/') },
      { title: t(lang, 'nav.cluster.generalDiag'), href: lp('/clusters/general-diagnostics/') },
      { title: t(lang, 'nav.group.energy'), group: true },
      { title: t(lang, 'nav.cluster.elecPower'), href: lp('/clusters/electrical-power-measurement/') },
      { title: t(lang, 'nav.viewAll82'), href: lp('/clusters/'), dividerBefore: true },
    ],
  },
  {
    title: t(lang, 'nav.dataTools'),
    children: [
      { title: t(lang, 'nav.jsonParser'), href: lp('/tools/json-parser/') },
    ],
  },
  { title: t(lang, 'nav.resources'), href: lp('/resources/') },
  {
    title: t(lang, 'nav.officialStandards'),
    children: [
      { title: t(lang, 'nav.csaWebsite'), href: 'https://csa-iot.org/', external: true },
      { title: t(lang, 'nav.matterSpec'), href: 'https://csa-iot.org/all-solutions/matter/', external: true },
      { title: t(lang, 'nav.matterGithub'), href: 'https://github.com/project-chip/connectedhomeip', external: true },
      { title: t(lang, 'nav.deviceLibrary'), href: 'https://csa-iot.org/csa-iot/csa-device-library/', external: true },
    ],
  },
];

// Strip lang prefix for matching active state
const pathForActive = stripLangPrefix(currentPath);

function isActive(href: string | undefined, current: string): boolean {
  if (!href) return false;
  const clean = stripLangPrefix(href);
  if (clean === '/') return current === '/';
  return current.startsWith(clean);
}

function isDropdownActive(item: NavItem, current: string): boolean {
  if (item.children) {
    return item.children.some(child => !child.external && isActive(child.href, current));
  }
  return false;
}
---

<header class="header">
  <div class="header-inner">
    <!-- Logo -->
    <a href={lp('/')} class="logo">
      <svg class="logo-icon" width="24" height="24" viewBox="0 0 120 120" fill="none" xmlns="http://www.w3.org/2000/svg">
        <polygon points="60,18 92,36 92,72 60,90 28,72 28,36" stroke="currentColor" stroke-width="5"/>
        <text x="60" y="68" text-anchor="middle" font-family="system-ui,-apple-system,sans-serif" font-size="36" font-weight="700" fill="currentColor" letter-spacing="-2">M</text>
        <circle cx="60" cy="18" r="5" fill="currentColor" opacity="0.7"/>
        <circle cx="92" cy="36" r="5" fill="currentColor" opacity="0.7"/>
        <circle cx="92" cy="72" r="5" fill="currentColor" opacity="0.7"/>
        <circle cx="60" cy="90" r="5" fill="currentColor" opacity="0.7"/>
        <circle cx="28" cy="72" r="5" fill="currentColor" opacity="0.7"/>
        <circle cx="28" cy="36" r="5" fill="currentColor" opacity="0.7"/>
      </svg>
      <span class="logo-text">Matter Learn</span>
    </a>

    <!-- Desktop nav -->
    <nav class="desktop-nav">
      {navigation.map((item) =>
        item.children ? (
          <div class:list={['dropdown', { 'mega': item.megaMenu }]}>
            <button
              class:list={['nav-item', 'dropdown-trigger', { active: isDropdownActive(item, pathForActive) }]}
              aria-haspopup="true"
              aria-expanded="false"
            >
              {item.title}
              <ChevronDown class="chevron" size={12} />
            </button>
            <div class:list={['dropdown-menu', { 'mega-menu': item.megaMenu }]}>
              {item.children.map((child) => (
                <Fragment>
                  {child.dividerBefore && <div class="dropdown-divider"></div>}
                  {child.group ? (
                    <div class="dropdown-group">{child.title}</div>
                  ) : (
                    <a
                      href={child.href}
                      class:list={['dropdown-item', { active: !child.external && isActive(child.href, pathForActive) }]}
                      {...(child.external ? { target: '_blank', rel: 'noopener noreferrer' } : {})}
                    >
                      <span>{child.title}</span>
                      {child.external && (
                        <ArrowUpRight class="external-icon" size={12} strokeWidth={1.5} />
                      )}
                    </a>
                  )}
                </Fragment>
              ))}
            </div>
          </div>
        ) : (
          <a
            href={item.href}
            class:list={['nav-item', { active: isActive(item.href, pathForActive) }]}
          >
            {item.title}
          </a>
        )
      )}
    </nav>

    <!-- Right side: language switch + theme toggle + mobile hamburger -->
    <div class="header-right">
      <a href={altPath} class="lang-btn" aria-label={t(lang, 'header.switchLangLabel')} data-lang-switch>
        <Globe size={15} />
        <span>{t(lang, 'header.switchLang')}</span>
      </a>
      <button id="theme-toggle" class="icon-btn" aria-label={t(lang, 'header.toggleTheme')}>
        <Sun id="sun-icon" size={18} />
        <Moon id="moon-icon" size={18} style="display:none;" />
      </button>
      <button id="mobile-menu-toggle" class="icon-btn mobile-only" aria-label={t(lang, 'header.toggleMenu')} aria-expanded="false">
        <Menu id="hamburger-icon" size={20} />
        <X id="close-icon" size={20} style="display:none;" />
      </button>
    </div>
  </div>

  <!-- Mobile nav panel -->
  <div id="mobile-nav" class="mobile-nav">
    {navigation.map((item) =>
      item.children ? (
        <div class="mobile-section">
          <button class="mobile-section-toggle" data-expanded="false">
            <span>{item.title}</span>
            <ChevronDown class="mobile-chevron" size={14} />
          </button>
          <div class="mobile-section-children">
            {item.children.map((child) => (
              <Fragment>
                {child.dividerBefore && <div class="mobile-divider"></div>}
                {child.group ? (
                  <div class="mobile-group">{child.title}</div>
                ) : (
                  <a
                    href={child.href}
                    class:list={['mobile-link', 'mobile-child', { active: !child.external && isActive(child.href, pathForActive) }]}
                    {...(child.external ? { target: '_blank', rel: 'noopener noreferrer' } : {})}
                  >
                    <span>{child.title}</span>
                    {child.external && (
                      <ArrowUpRight class="external-icon" size={12} strokeWidth={1.5} />
                    )}
                  </a>
                )}
              </Fragment>
            ))}
          </div>
        </div>
      ) : (
        <a
          href={item.href}
          class:list={['mobile-link', { active: isActive(item.href, pathForActive) }]}
        >
          {item.title}
        </a>
      )
    )}
    <!-- Mobile language switch -->
    <div class="mobile-divider" style="margin: 0.5rem 0;"></div>
    <a href={altPath} class="mobile-link" data-lang-switch>
      <span>{lang === 'zh' ? 'English' : '中文'}</span>
      <Globe size={14} />
    </a>
  </div>
</header>

<script>
function updateThemeIcons() {
  const isDark = document.documentElement.classList.contains('dark');
  const sun = document.getElementById('sun-icon') as HTMLElement | null;
  const moon = document.getElementById('moon-icon') as HTMLElement | null;
  if (sun && moon) {
    sun.style.display = isDark ? 'none' : 'block';
    moon.style.display = isDark ? 'block' : 'none';
  }
}

let docClickAc: AbortController | undefined;

document.addEventListener('astro:page-load', () => {
  updateThemeIcons();

  // Language preference
  document.querySelectorAll('[data-lang-switch]').forEach((el) => {
    el.addEventListener('click', () => {
      const href = el.getAttribute('href') || '';
      const m = href.match(/^\/(zh|en)\//);
      if (m) { try { localStorage.setItem('preferredLang', m[1]); } catch {} }
    });
  });

  // Theme toggle
  document.getElementById('theme-toggle')?.addEventListener('click', () => {
    document.documentElement.classList.toggle('dark');
    const isDark = document.documentElement.classList.contains('dark');
    localStorage.setItem('theme', isDark ? 'dark' : 'light');
    updateThemeIcons();
  });

  // Mobile menu
  const mobileBtn = document.getElementById('mobile-menu-toggle');
  const mobileNav = document.getElementById('mobile-nav');
  const hamburger = document.getElementById('hamburger-icon') as HTMLElement | null;
  const closeBtn = document.getElementById('close-icon') as HTMLElement | null;
  if (mobileBtn && mobileNav) {
    mobileNav.classList.remove('open');
    mobileBtn.setAttribute('aria-expanded', 'false');
    if (hamburger) hamburger.style.display = 'block';
    if (closeBtn) closeBtn.style.display = 'none';
    mobileBtn.addEventListener('click', () => {
      const isOpen = mobileNav.classList.toggle('open');
      mobileBtn.setAttribute('aria-expanded', isOpen ? 'true' : 'false');
      if (hamburger) hamburger.style.display = isOpen ? 'none' : 'block';
      if (closeBtn) closeBtn.style.display = isOpen ? 'block' : 'none';
    });
  }

  // Mobile section toggles
  document.querySelectorAll('.mobile-section-toggle').forEach((btn) => {
    btn.addEventListener('click', function (this: HTMLElement) {
      const expanded = this.getAttribute('data-expanded') === 'true';
      this.setAttribute('data-expanded', expanded ? 'false' : 'true');
      this.classList.toggle('expanded');
      const children = this.nextElementSibling as HTMLElement | null;
      if (children) children.classList.toggle('open');
    });
  });

  // Desktop dropdown hover
  document.querySelectorAll('.dropdown').forEach((dropdown) => {
    const trigger = dropdown.querySelector('.dropdown-trigger');
    dropdown.addEventListener('mouseenter', () => {
      trigger?.setAttribute('aria-expanded', 'true');
      dropdown.classList.add('open');
    });
    dropdown.addEventListener('mouseleave', () => {
      trigger?.setAttribute('aria-expanded', 'false');
      dropdown.classList.remove('open');
    });
    trigger?.addEventListener('click', (e) => {
      e.preventDefault();
      const isOpen = dropdown.classList.toggle('open');
      trigger.setAttribute('aria-expanded', isOpen ? 'true' : 'false');
    });
  });

  // Close dropdowns on outside click (one listener on document, replaced each nav)
  if (docClickAc) docClickAc.abort();
  docClickAc = new AbortController();
  document.addEventListener('click', (e) => {
    document.querySelectorAll('.dropdown.open').forEach((d) => {
      if (!d.contains(e.target as Node)) {
        d.classList.remove('open');
        d.querySelector('.dropdown-trigger')?.setAttribute('aria-expanded', 'false');
      }
    });
  }, { signal: docClickAc.signal });
});
</script>

<style>
  .header {
    position: fixed;
    top: 0;
    left: 0;
    right: 0;
    z-index: 50;
    height: 60px;
    background-color: var(--bg-primary);
    border-bottom: 1px solid var(--border-color);
    backdrop-filter: blur(12px);
    background-color: color-mix(in srgb, var(--bg-primary), transparent 5%);
  }

  .header-inner {
    display: flex;
    align-items: center;
    height: 100%;
    padding: 0 1.5rem;
    max-width: 80rem;
    margin: 0 auto;
    gap: 2rem;
  }

  .logo {
    display: flex;
    align-items: center;
    gap: 0.5rem;
    text-decoration: none;
    color: var(--text-primary);
    flex-shrink: 0;
  }

  .logo-icon {
    color: var(--color-primary-600, #4f46e5);
  }

  :global(.dark) .logo-icon {
    color: var(--color-primary-400, #818cf8);
  }

  .logo-text {
    font-size: 1.125rem;
    font-weight: 700;
    letter-spacing: -0.025em;
  }

  .desktop-nav {
    display: flex;
    align-items: center;
    gap: 0.25rem;
    flex: 1;
  }

  .nav-item {
    display: inline-flex;
    align-items: center;
    gap: 0.25rem;
    padding: 0.5rem 0.75rem;
    font-size: 0.875rem;
    font-weight: 500;
    color: var(--text-secondary);
    text-decoration: none;
    border-radius: 0.375rem;
    border: none;
    background: none;
    cursor: pointer;
    transition: color 0.15s ease, background-color 0.15s ease;
    white-space: nowrap;
    font-family: inherit;
  }

  .nav-item:hover {
    color: var(--text-primary);
    background: var(--bg-hover);
  }

  .nav-item.active {
    color: var(--color-primary-600, #4f46e5);
  }

  :global(.dark) .nav-item.active {
    color: var(--color-primary-400, #818cf8);
  }

  .chevron {
    transition: transform 0.2s ease;
    opacity: 0.6;
  }

  .dropdown {
    position: relative;
  }

  .dropdown-menu {
    position: absolute;
    top: calc(100% + 4px);
    left: 0;
    min-width: 200px;
    padding: 0.375rem;
    background: var(--bg-primary);
    border: 1px solid var(--border-color);
    border-radius: 0.5rem;
    box-shadow: 0 4px 16px rgba(0, 0, 0, 0.08), 0 1px 4px rgba(0, 0, 0, 0.04);
    opacity: 0;
    visibility: hidden;
    transform: translateY(-4px);
    transition: opacity 0.15s ease, transform 0.15s ease, visibility 0.15s ease;
    z-index: 60;
  }

  :global(.dark) .dropdown-menu {
    box-shadow: 0 4px 16px rgba(0, 0, 0, 0.3), 0 1px 4px rgba(0, 0, 0, 0.15);
  }

  .dropdown.open .dropdown-menu {
    opacity: 1;
    visibility: visible;
    transform: translateY(0);
  }

  .dropdown.open .chevron {
    transform: rotate(180deg);
  }

  .dropdown-item {
    display: flex;
    align-items: center;
    justify-content: space-between;
    gap: 0.5rem;
    padding: 0.5rem 0.75rem;
    font-size: 0.8125rem;
    color: var(--text-secondary);
    text-decoration: none;
    border-radius: 0.375rem;
    transition: background-color 0.1s ease, color 0.1s ease;
  }

  .dropdown-item:hover {
    background: var(--bg-hover);
    color: var(--text-primary);
  }

  .dropdown-item.active {
    color: var(--color-primary-600, #4f46e5);
    background: var(--color-primary-50, #eef2ff);
  }

  :global(.dark) .dropdown-item.active {
    color: var(--color-primary-400, #818cf8);
    background: rgba(99, 102, 241, 0.1);
  }

  .dropdown-group {
    padding: 0.5rem 0.75rem 0.25rem;
    font-size: 0.6875rem;
    font-weight: 700;
    text-transform: uppercase;
    letter-spacing: 0.05em;
    color: var(--color-primary-600, #4f46e5);
  }

  :global(.dark) .dropdown-group {
    color: var(--color-primary-400, #818cf8);
  }

  .dropdown-group:not(:first-child) {
    margin-top: 0.25rem;
    border-top: 1px solid var(--border-color);
    padding-top: 0.5rem;
  }

  .mega-menu {
    columns: 3;
    column-gap: 0.25rem;
    min-width: 540px;
    max-height: calc(100vh - 80px);
    overflow-y: auto;
  }

  .mega-menu .dropdown-group,
  .mega-menu .dropdown-item,
  .mega-menu .dropdown-divider {
    break-inside: avoid;
  }

  .dropdown-divider {
    height: 1px;
    margin: 0.25rem 0.5rem;
    background: var(--border-color);
  }

  .external-icon {
    flex-shrink: 0;
    opacity: 0.5;
  }

  .header-right {
    display: flex;
    align-items: center;
    gap: 0.25rem;
    margin-left: auto;
    flex-shrink: 0;
  }

  .lang-btn {
    display: inline-flex;
    align-items: center;
    gap: 0.25rem;
    padding: 0.375rem 0.625rem;
    font-size: 0.8125rem;
    font-weight: 500;
    color: var(--text-secondary);
    text-decoration: none;
    border-radius: 0.375rem;
    border: 1px solid var(--border-color);
    background: none;
    cursor: pointer;
    transition: color 0.15s ease, background-color 0.15s ease, border-color 0.15s ease;
    white-space: nowrap;
  }

  .lang-btn:hover {
    background: var(--bg-hover);
    color: var(--text-primary);
    border-color: var(--border-active);
  }

  .icon-btn {
    display: flex;
    align-items: center;
    justify-content: center;
    width: 2.25rem;
    height: 2.25rem;
    border-radius: 0.375rem;
    border: none;
    background: none;
    color: var(--text-secondary);
    cursor: pointer;
    transition: color 0.15s ease, background-color 0.15s ease;
  }

  .icon-btn:hover {
    background: var(--bg-hover);
    color: var(--text-primary);
  }

  .mobile-only {
    display: none;
  }

  .mobile-nav {
    display: none;
    position: absolute;
    top: 60px;
    left: 0;
    right: 0;
    background: var(--bg-primary);
    border-bottom: 1px solid var(--border-color);
    padding: 0.5rem 1rem 1rem;
    max-height: calc(100vh - 60px);
    overflow-y: auto;
    box-shadow: 0 8px 24px rgba(0, 0, 0, 0.08);
  }

  :global(.dark) .mobile-nav {
    box-shadow: 0 8px 24px rgba(0, 0, 0, 0.3);
  }

  .mobile-nav.open {
    display: block;
  }

  .mobile-link {
    display: flex;
    align-items: center;
    justify-content: space-between;
    padding: 0.625rem 0.75rem;
    font-size: 0.9375rem;
    color: var(--text-secondary);
    text-decoration: none;
    border-radius: 0.375rem;
    transition: background-color 0.1s ease;
  }

  .mobile-link:hover {
    background: var(--bg-hover);
  }

  .mobile-link.active {
    color: var(--color-primary-600, #4f46e5);
    background: var(--color-primary-50, #eef2ff);
  }

  :global(.dark) .mobile-link.active {
    color: var(--color-primary-400, #818cf8);
    background: rgba(99, 102, 241, 0.1);
  }

  .mobile-child {
    padding-left: 1.5rem;
    font-size: 0.875rem;
  }

  .mobile-section-toggle {
    display: flex;
    align-items: center;
    justify-content: space-between;
    width: 100%;
    padding: 0.625rem 0.75rem;
    font-size: 0.9375rem;
    font-weight: 500;
    color: var(--text-secondary);
    background: none;
    border: none;
    border-radius: 0.375rem;
    cursor: pointer;
    font-family: inherit;
    transition: background-color 0.1s ease;
  }

  .mobile-section-toggle:hover {
    background: var(--bg-hover);
  }

  .mobile-chevron {
    transition: transform 0.2s ease;
    opacity: 0.5;
  }

  .mobile-section-toggle.expanded .mobile-chevron {
    transform: rotate(180deg);
  }

  .mobile-section-children {
    display: none;
    padding-bottom: 0.25rem;
  }

  .mobile-section-children.open {
    display: block;
  }

  .mobile-group {
    padding: 0.5rem 1.5rem 0.125rem;
    font-size: 0.6875rem;
    font-weight: 700;
    text-transform: uppercase;
    letter-spacing: 0.05em;
    color: var(--color-primary-600, #4f46e5);
  }

  :global(.dark) .mobile-group {
    color: var(--color-primary-400, #818cf8);
  }

  .mobile-group:not(:first-child) {
    margin-top: 0.25rem;
    border-top: 1px solid var(--border-color);
    padding-top: 0.5rem;
  }

  .mobile-divider {
    height: 1px;
    margin: 0.25rem 1.5rem;
    background: var(--border-color);
  }

  @media (max-width: 768px) {
    .header-inner {
      padding: 0 1rem;
    }

    .desktop-nav {
      display: none;
    }

    .mobile-only {
      display: flex;
    }
  }
</style>
